<%# locals: (category:) %>
<h2 id="restock_category_name_<%= category.id %>"
    class="text-xl font-semibold text-olive-800 dark:text-[#e8ede3] truncate"
    style="font-family: 'Instrument Serif', serif;">
  <%= category.name %>
</h2>
